# The production app this repro was extracted from uses yarn, which does not
# enforce peer ranges the way npm does. expo-router's optional web peers
# (vaul -> react-dom) trip npm's ERESOLVE on a native-only project.
legacy-peer-deps=true
